
        "use strict";
        //SECTION: METHODS OF PRIMITIVE:
            /*
            can i add string Property:yes we can change string properties by using the object wrappers and the leave it as 
            primitive after asccessing it.but we can not add string properties.
            */
            //===================================================================
        //SECTION:NUMBERS:
            /*
             sum numbers from visitor: asks the user two enter two numbers and return the sum.
            */
            //==============================================================
            /*
             sum numbers from visitor: asks the user two enter two numbers and return the sum.
            */
            //==============================================================
            /*
            repeat the input untill is a number: lets the code below.
            */
            /**
             * @param  {number} num is
             * @returns {number} total
             */
            function readNumber(num) {
                     num;

                    do {
                        num = prompt("Enter a number please?", 0);
                    } while (!isFinite(num));

                    if (num === null || num === " ") return null;

                    return +num;
                }

                alert(`Read: ${readNumber()}`);
             //==================================================================
             /*
             occassional infinite loop: this is a loop that do not terminate.
             */
             //==================================================================
             /*
             the random number from min to max prints floating number between the max and min but excluding the
             max.
             */
             //==================================================================
    //SECTION STRING:
            /*
            */
            
